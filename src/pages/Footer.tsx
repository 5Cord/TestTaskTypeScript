import cl from './footer.module.css';

export default function Footer(): JSX.Element {
    return (
        <footer className={cl.footer}>
            <div className={cl.headFooter}>Some Company 2024</div>
        </footer>
    );
}
